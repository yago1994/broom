{
  "name": "broom",
  "version": "0.1.0",
  "private": true,
  "description": "Agent-driven UI modifier — point, describe, persist.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.260",
    "esbuild": "^0.20.0",
    "typescript": "^5.4.0"
  }
}
